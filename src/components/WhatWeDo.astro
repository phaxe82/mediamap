---
const services = [
  {
    icon: '🎬',
    title: 'Production',
    description:
      'Scripting, shot planning, scheduling, on-set workflows. We map the AI tools that save time before the camera ever rolls.',
  },
  {
    icon: '✂️',
    title: 'Post Production',
    description:
      'Editing, colour grading, VFX, sound design, subtitling, delivery. The tools that actually work — tested by people who actually use them.',
  },
  {
    icon: '🧠',
    title: 'Stay Human',
    description:
      "AI is a tool, not a replacement. We help you use it *with* your craft — so your creative voice stays yours.",
  },
];
---

<section id="what-we-do" class="py-24 px-6 border-t border-white/5">
  <div class="max-w-6xl mx-auto">
    <!-- Header -->
    <div class="max-w-2xl mb-16">
      <p class="text-[#f5a623] text-sm font-semibold uppercase tracking-widest mb-4">
        What We Do
      </p>
      <h2 class="text-4xl md:text-5xl font-black tracking-tight mb-4">
        We let creators get back<br />to <span class="text-[#f5a623]">creating.</span>
      </h2>
      <p class="text-white/50 text-lg leading-relaxed">
        Media Map is an AI consulting service built specifically for the media industry. No generic
        advice. No hype. Just practical guidance from people who've been on set and in the suite.
      </p>
    </div>

    <!-- Cards -->
    <div class="grid md:grid-cols-3 gap-6">
      {
        services.map((s) => (
          <div class="bg-white/[0.03] border border-white/5 rounded-2xl p-8 hover:border-white/10 transition-colors">
            <div class="text-4xl mb-5">{s.icon}</div>
            <h3 class="text-xl font-bold mb-3">{s.title}</h3>
            <p class="text-white/50 leading-relaxed">{s.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
